public void MoveZeroes(int[] nums) {
    int n = 0;
        
    for (int i = 0; i < nums.Length; i++)
    {
        if(nums[i] != 0)
        {
            nums[n] = nums[i];
            n++;
        }
    }
    for (int i = n; i < nums.Length; i++)
    {
        nums[i] = 0;
    }
}
